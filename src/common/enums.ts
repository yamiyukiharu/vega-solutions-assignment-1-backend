export enum Protocol {
  UniswapV3 = 'uniswapv3',
}

export enum Pool {
  Usdc_eth = 'usdc_eth',
}

export enum ReportStatus {
  Pending = 'pending',
  InProgress = 'in_progress',
  Completed = 'completed',
  Failed = 'failed',
}